# Ignore Terraform state files and backup files
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.backup

# Ignore crash logs
crash.log

# Ignore Terraform lock file (for providers)
.terraform.lock.hcl

# Ignore Terraform working directory
.terraform/

# Ignore generated `.tfvars` files (often contain sensitive data)

# Ignore plan output files
*.tfplan

# Ignore any `.terraformrc` or `terraform.rc` (if used for credentials)
.terraformrc
terraform.rc
